import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#fff",
      contrastText: "#FFFFFF"
    },
    secondary: {
      main: "#898CA9",
      contrastText: "#898CA9"
    },
  }
});

export default theme;